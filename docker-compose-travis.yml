version: '3.7'

services:
  web:
    env_file: 
      - docker/environments/django.env.example
      - docker/environments/database.env.example
  postgres:
    env_file:
      - docker/environments/database.env.example
